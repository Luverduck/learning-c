// 16.1 전처리기가 해주는 일들
/*
	컴파일 (Compile)
	작성한 코드를 실행 파일로 만드는 과정을 컴파일(Compile)이라 한다.
	( Visual Studio에서는 빌드(Build)라 한다. )
	
	컴파일 과정
	코드 작성 -> 전처리기 -> 컴파일러 -> 링커 -> 실행 파일 생성

	전처리기 (Pre-Processor)
	코드를 컴파일러에서 컴파일하기 전의 필요한 처리를 담당한다.
*/

#include <stdio.h>
int main(void)
{

	return 0;
}