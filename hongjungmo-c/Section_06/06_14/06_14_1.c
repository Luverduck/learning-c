// 6.14 어떤 루프를 사용할까?
/*
	진입 조건으로 구현하는 루프
	- for문 : 반복 횟수를 정확하게 알고있을 경우 사용
		for ( int i = 0; i < 100; i ++ )
		
	- while문 : 특정 조건을 만족할 때까지 무한히 반복하는 경우 사용
		while ( scanf("%d", &num) == 1 )

	탈출 조건으로 구현하는 루프
	- do ~ while문 : 조건문과 실행문을 분리 + 최소 한 번 이상 실행할 경우 사용
*/

#include <stdio.h>
int main(void)
{

	return 0;
}